import React from 'react';
import styles from '../styles/footer.module.scss'

const Footer: React.FC = () => {
    return (
        <footer className={styles.footer}>
            <h1 className={styles.footerText}>Footer</h1>
        </footer>
    )
}

export default Footer;
